// # Ghost bootloader
// Orchestrates the loading of Ghost
// When run from command line.

var when      = require('when');

process.env.NODE_ENV = process.env.NODE_ENV || 'development';

function startSpide(options) {

}

module.exports = startSpide;